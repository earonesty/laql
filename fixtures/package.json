{
  "name": "@laql/fixtures",
  "version": "0.0.1",
  "private": true,
  "description": "Deterministic test fixtures for LaQL: generated Parquet files and Iceberg warehouse layouts",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "node src/generate.ts",
    "fetch:external": "node src/fetch-external.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "hyparquet-writer": "^0.15.0"
  }
}
